#syntax
#lambda par1,par2,par3,...parn:expression

add = lambda a,b:a+b # we can pass n number of parameters but only one expression can be return

print(add(5,5))

#map(),filter(),sort() will perform the oepration all the columns